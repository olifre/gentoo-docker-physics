machine:
  services:
    - docker

dependencies:
  cache_directories:
    - ~/docker
    - ~/.ccache-root
    - ~/packages-root
  override:
    - sudo apt-get install -y ccache
    - docker info
    - CCACHE_DIR=~/.ccache-${CIRCLE_BRANCH}/ ccache -s
    - CCACHE_DIR=~/.ccache-${CIRCLE_BRANCH}/ ccache -z
    - du -sh ~/packages-${CIRCLE_BRANCH}/*/*.tbz2 | sort -hr
    - timeout 7000s ./build-docker.sh olifre/gentoo-docker-physics.prep-root olifre/gentoo-docker-physics.${CIRCLE_BRANCH} ${CIRCLE_BRANCH}:
        timeout: 10800
    - CCACHE_DIR=~/.ccache-${CIRCLE_BRANCH}/ ccache -s
    - du -sh ~/packages-${CIRCLE_BRANCH}/*/*.tbz2 | sort -hr

test:
  override:
    - /bin/true

deployment:
  hub:
    branch: /.*/
    commands:
      - docker login -e $DOCKER_EMAIL -u $DOCKER_USER -p $DOCKER_PASS
      - docker images -a --no-trunc
      - ./push-it-and-next.sh ${CIRCLE_BRANCH} geant ${CIRCLE_TOKEN}
