machine:
  services:
    - docker

dependencies:
  cache_directories:
    - "~/docker"
    - "~/.ccache-debugging"
  override:
    - docker info
    - ./build-docker.sh olifre/gentoo-docker-physics.setup olifre/gentoo-docker-physics.debugging debugging:
        timeout: 10800

test:
  override:
    - /bin/true

deployment:
  hub:
    branch: /.*/
    commands:
      - docker login -e $DOCKER_EMAIL -u $DOCKER_USER -p $DOCKER_PASS
      - docker push olifre/gentoo-docker-physics.debugging || docker push olifre/gentoo-docker-physics.debugging
